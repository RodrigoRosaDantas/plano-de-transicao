let DATA=null,currentView='home',deferredPrompt=null,searchIndex=[];
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const fmt=n=>new Intl.NumberFormat('pt-BR').format(Number(n||0));
const pct=n=>`${Number(n||0).toFixed(2).replace('.',',')}%`;
const money=n=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(n||0));
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const dateBR=d=>d?String(d).slice(0,10).split('-').reverse().join('/'):'—';
function toast(msg){const el=$('#toast');el.textContent=msg;el.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>el.classList.remove('show'),2400)}
function statusTone(text=''){const s=text.toLowerCase();return s.includes('andamento')||s.includes('parcial')?'warn':s.includes('próxima')?'info':'good'}
function countdown(){const diff=new Date(DATA.meta.nextExam)-new Date();if(diff<=0)return{days:0,hours:0,mins:0,total:0};return{days:Math.floor(diff/86400000),hours:Math.floor(diff%86400000/3600000),mins:Math.floor(diff%3600000/60000),total:diff}}
function progress(value,max=100){return Math.max(0,Math.min(100,max?Number(value)/Number(max)*100:0))}
function iconFor(view){return({home:'⌂',journey:'◎',exams:'◈',finance:'◉',performance:'↗',audit:'◇',strategy:'✦'})[view]||'•'}
function title(name,desc,meta=''){return `<div class="section-title"><div><span class="section-kicker">${iconFor(currentView)} ${esc(meta||'PLANO DE TRANSIÇÃO')}</span><h1>${esc(name)}</h1><p>${esc(desc)}</p></div></div>`}
function metric(label,value,sub='',opts={}){const p=opts.progress;return `<article class="metric-card ${opts.tone||''}"><div class="metric-top"><span class="label">${esc(label)}</span>${opts.badge?`<span class="mini-badge ${opts.badgeTone||''}">${esc(opts.badge)}</span>`:''}</div><div class="value">${value}</div><div class="sub">${sub}</div>${p!=null?`<div class="progress"><i style="width:${Math.min(100,p)}%"></i></div>`:''}</article>`}
function flowStep(label,sub,state=''){return `<div class="flow-step ${state}"><span class="flow-node"></span><div><strong>${esc(label)}</strong><small>${esc(sub)}</small></div></div>`}
function examCard(e){return `<article class="exam-card"><div class="exam-head"><div><span class="eyebrow">${esc(e.role)}</span><h3>${esc(e.name)}</h3></div><span class="pill ${statusTone(e.status)}">${esc(e.status.includes('Próxima')?'Próxima prova':e.status.includes('andamento')?'Em andamento':'Resultado registrado')}</span></div><div class="exam-kpis"><div><small>Data</small><strong>${dateBR(e.date)}</strong></div><div><small>Resultado</small><strong>${esc(e.score)}</strong></div><div><small>Aproveitamento</small><strong>${e.rawAccuracy==null?'—':pct(e.rawAccuracy)}</strong></div><div><small>Nota editalícia</small><strong>${esc(e.weightedScore||'—')}</strong></div></div><div class="exam-rank"><small>Classificação / etapa</small><strong>${esc(e.ranking||'—')}</strong></div><details><summary>Ver contexto auditado</summary><p>${esc(e.status||'Sem observação adicional.')}</p></details></article>`}
function home(){const m=DATA.metrics,c=countdown(),tdasProg=progress(m.tdas.stepsDone,m.tdas.stepsTotal);const latest=DATA.historyCycles.find(x=>x.name.includes('TDAS'))||DATA.historyCycles[0];return `${title('Visão executiva','O projeto completo: jornada, investimento, provas, desempenho e decisão seguinte.','COMANDO DA TRANSIÇÃO')}
<section class="hero executive-hero"><div class="hero-card hero-primary"><div class="hero-grid"><div><div class="eyebrow">MISSÃO ATUAL · RETA FINAL SEDES/DF</div><h2>Converter capital de estudo em classificação competitiva.</h2><p>${esc(DATA.mission)}</p><div class="hero-actions"><button class="action-chip" data-nav="performance">↗ Ver desempenho</button><button class="action-chip" data-nav="exams">◈ Ver provas reais</button><button class="action-chip" data-nav="strategy">✦ Ver decisão seguinte</button></div></div><div class="hero-signal"><span class="signal-label">Prioridade operacional</span><strong>TDAS 202</strong><small>${m.tdas.stepsDone}/${m.tdas.stepsTotal} etapas executadas</small><div class="progress large"><i style="width:${tdasProg}%"></i></div><span class="signal-foot">EDAS 400 permanece separado e estruturado em paralelo.</span></div></div></div>
<div class="panel countdown-card"><div class="eyebrow">SEDES/DF · 06/09/2026</div><div class="countdown-number">${c.days}</div><strong>dias</strong><div class="countdown-sub">${c.hours}h ${c.mins}min restantes</div><div class="countdown-divider"></div><small>Corte vivo sincronizado com o Notion</small></div></section>
<div class="grid metrics executive-metrics">${metric('TDAS 202',pct(m.tdas.accuracy),`${fmt(m.tdas.questions)} questões · ${fmt(m.tdas.hits)} acertos`,{progress:m.tdas.accuracy,badge:`${m.tdas.stepsDone}/${m.tdas.stepsTotal} etapas`,badgeTone:'good'})}${metric('EDAS 400',pct(m.edas.accuracy),`${fmt(m.edas.questions)} questões · ${fmt(m.edas.hits)} acertos`,{progress:m.edas.accuracy,badge:'trilha separada'})}${metric('Capital mensurável',fmt(m.history.questions),`${fmt(m.history.hits)} acertos · ${fmt(m.history.errors)} erros`,{progress:m.history.accuracy,badge:pct(m.history.accuracy),badgeTone:'good'})}${metric('Investimento SEDES',money(m.finance.sedesConfirmed),`Status: ${esc(m.finance.status)}`,{badge:'confirmado',badgeTone:'warn'})}</div>
<section class="panel journey-flow"><div class="panel-head"><div><span class="eyebrow">JORNADA, CONCURSOS E INVESTIMENTO</span><h2>Do esforço à decisão de carreira</h2></div><button class="text-button" data-nav="journey">Abrir jornada →</button></div><div class="flow-track">${flowStep('Preparação','03/07/2025','done')}${flowStep('Investimento',money(m.finance.sedesConfirmed),'done')}${flowStep('Prova','06/09/2026','active')}${flowStep('Desempenho',pct(m.history.accuracy),'done')}${flowStep('Classificação','Após resultado','future')}${flowStep('Decisão seguinte','Carreira aderente','future')}</div></section>
<div class="dashboard-grid"><section class="panel focus-panel"><div class="panel-head"><div><span class="eyebrow">ESTADO OPERACIONAL</span><h2>O que importa agora</h2></div><span class="health-dot">● saudável</span></div><div class="focus-list"><div class="focus-item"><span class="focus-rank">01</span><div><strong>Fechar TDAS com qualidade</strong><p>${m.tdas.stepsDone}/${m.tdas.stepsTotal} etapas · ${m.tdas.simulations} simulados · ${m.tdas.errorNotebook} registros no caderno de erros.</p></div></div><div class="focus-item"><span class="focus-rank">02</span><div><strong>Preservar EDAS sem contaminar métricas</strong><p>${fmt(m.edas.questions)} questões · ${fmt(m.edas.errorNotebook)} erros catalogados · ${fmt(m.edas.caseStudies)} estudos de caso.</p></div></div><div class="focus-item"><span class="focus-rank">03</span><div><strong>Decidir depois da prova com base em aderência</strong><p>Reaproveitamento, chance de nomeação, remuneração, qualidade de vida, localização e custo de oportunidade.</p></div></div></div></section>
<section class="panel signal-panel"><div class="panel-head"><div><span class="eyebrow">SINAIS EXECUTIVOS</span><h2>Leitura rápida</h2></div></div><div class="signal-grid"><div><small>Último ciclo principal</small><strong>${esc(latest?.name||'TDAS')}</strong><span>${latest?pct(latest.accuracy):'—'}</span></div><div><small>Questões sem resultado</small><strong>${fmt(m.history.withoutResult)}</strong><span>fora do aproveitamento</span></div><div><small>Redações TDAS</small><strong>${fmt(m.tdas.essays)}</strong><span>${fmt(m.tdas.essaysGraded)} avaliadas</span></div><div><small>Sincronização</small><strong>${DATA.meta.live?'AO VIVO':'SNAPSHOT'}</strong><span>${DATA.meta.syncWarnings?.length?'com ressalvas':'sem alertas'}</span></div></div></section></div>
<section class="section-block"><div class="panel-head"><div><span class="eyebrow">PROVAS REAIS COMO RÉGUA</span><h2>Resultado observado, não sensação</h2></div><button class="text-button" data-nav="exams">Ver histórico completo →</button></div><div class="exam-grid compact">${DATA.exams.slice(0,2).map(examCard).join('')}</div></section>`}
function journey(){const m=DATA.metrics;return `${title('Jornada e marcos','A construção da transição como projeto contínuo, sem reiniciar a cada edital.','01 · JORNADA E MARCOS')}<div class="journey-summary grid metrics">${metric('Início da jornada','03/07/2025','Retomada estruturada dos estudos.')}${metric('Capital mensurável',fmt(m.history.questions),`${pct(m.history.accuracy)} de aproveitamento`,{progress:m.history.accuracy})}${metric('Provas reais registradas',fmt(DATA.exams.filter(e=>e.rawAccuracy!=null).length),'Régua externa de desempenho.')}${metric('Próximo marco','06/09/2026','SEDES/DF · TDAS 202 + EDAS 400')}</div><div class="journey-layout"><section class="panel"><div class="panel-head"><div><span class="eyebrow">LINHA DO TEMPO</span><h2>Marcos que mudaram a trajetória</h2></div></div><div class="timeline rich">${DATA.timeline.map((x,i)=>`<div class="timeline-item"><div class="timeline-index">${String(i+1).padStart(2,'0')}</div><div><small>${esc(x.date)}</small><b>${esc(x.title)}</b><span>${esc(x.detail)}</span></div></div>`).join('')}</div></section><aside class="panel journey-aside"><span class="eyebrow">LEITURA DA JORNADA</span><h2>Capital acumulado</h2><p>O objetivo desta página não é registrar dias isolados; é mostrar como estudo, prova, investimento e decisões se acumulam numa única transição de carreira.</p><div class="mini-stack"><div><small>Histórico bruto</small><strong>${fmt(m.history.rawRecords)}</strong></div><div><small>Mensurável</small><strong>${fmt(m.history.questions)}</strong></div><div><small>Sem resultado</small><strong>${fmt(m.history.withoutResult)}</strong></div></div><div class="callout"><b>Regra de continuidade</b><br>Não recomeçar a cada edital. Reaproveitar a base e deslocar esforço apenas onde houver ganho marginal real.</div></aside></div>`}
function exams(){return `${title('Concursos, provas e resultados','Prova, aproveitamento, nota editalícia, classificação e situação do certame permanecem separados.','02 · CONCURSOS, PROVAS E RESULTADOS')}<div class="exam-grid">${DATA.exams.map(examCard).join('')}</div><section class="panel comparison-panel"><div class="panel-head"><div><span class="eyebrow">LEITURA COMPARATIVA</span><h2>Evolução em provas reais</h2></div></div><div class="comparison-bars">${DATA.exams.filter(e=>e.rawAccuracy!=null).map(e=>`<div class="comparison-row"><div><strong>${esc(e.name)}</strong><small>${dateBR(e.date)} · ${esc(e.role)}</small></div><div class="bar-track"><div class="bar-fill" style="width:${e.rawAccuracy}%"></div></div><strong>${pct(e.rawAccuracy)}</strong></div>`).join('')}</div></section>`}
function finance(){const f=DATA.metrics.finance;const entries=Array.isArray(DATA.financeEntries)?DATA.financeEntries:[];return `${title('Custos e investimentos','O dinheiro entra no mesmo fluxo analítico de preparação, prova e decisão seguinte.','02.1 · CUSTOS E INVESTIMENTOS')}<div class="grid metrics">${metric('SEDES/DF confirmado',money(f.sedesConfirmed),'Somente valores confirmados entram no total.',{badge:'auditado',badgeTone:'good'})}${metric('Status financeiro',esc(f.status),'Ciclo permanece aberto enquanto houver itens não fechados.',{badge:statusTone(f.status)})}${metric('Regra de cálculo','Líquido','Pago − reembolso; rateio impede dupla contagem.')}${metric('Ausência de dado','≠ R$ 0,00','Campo sem evidência permanece não confirmado.')}</div>${entries.length?`<section class="panel"><div class="panel-head"><div><span class="eyebrow">COMPOSIÇÃO</span><h2>Lançamentos do ciclo</h2></div></div><div class="table-wrap"><table><thead><tr><th>Item</th><th>Categoria</th><th>Concurso</th><th>Valor</th><th>Status</th></tr></thead><tbody>${entries.map(x=>`<tr><td>${esc(x.name)}</td><td>${esc(x.category)}</td><td>${esc(x.exam)}</td><td>${money(x.value)}</td><td><span class="pill ${statusTone(x.status)}">${esc(x.status)}</span></td></tr>`).join('')}</tbody></table></div></section>`:''}<div class="dashboard-grid"><section class="panel"><span class="eyebrow">GOVERNANÇA FINANCEIRA</span><h2>Como o valor é tratado</h2><div class="rule-list"><div><span>01</span><p><b>Despesa direta</b> pertence ao respectivo concurso.</p></div><div><span>02</span><p><b>Despesa compartilhada</b> é registrada uma vez e distribuída por rateio.</p></div><div><span>03</span><p><b>Estimado, pago e reembolsado</b> permanecem separados.</p></div><div><span>04</span><p><b>Taxa prevista em edital</b> não vira gasto sem confirmação.</p></div></div></section><section class="panel"><span class="eyebrow">FLUXO DO INVESTIMENTO</span><h2>Preparação → resultado</h2><div class="finance-flow"><div><strong>Recurso</strong><small>inscrição, deslocamento, material, apoio</small></div><span>→</span><div><strong>Prova</strong><small>evento competitivo real</small></div><span>→</span><div><strong>Resultado</strong><small>nota, classificação, retorno estratégico</small></div></div><div class="callout"><b>Leitura correta:</b> investimento não é despesa isolada; é parte do custo de conversão da transição.</div></section></div>`}
function performance(){const m=DATA.metrics,max=Math.max(...DATA.historyCycles.map(x=>x.questions),1);const sorted=[...DATA.historyCycles].sort((a,b)=>b.questions-a.questions);return `${title('Desempenho e histórico','Volume, acertos, erros e aproveitamento reconciliados sem dupla contagem.','03 · DESEMPENHO E HISTÓRICO')}<div class="grid metrics">${metric('Questões mensuráveis',fmt(m.history.questions),`${fmt(m.history.rawRecords)} registros brutos`)}${metric('Acertos',fmt(m.history.hits),pct(m.history.accuracy),{progress:m.history.accuracy,badge:'consolidado',badgeTone:'good'})}${metric('Erros',fmt(m.history.errors),'Respostas incorretas no consolidado.')}${metric('Sem resultado',fmt(m.history.withoutResult),'Incluídas apenas no volume bruto.')}</div><div class="performance-split"><section class="panel"><div class="panel-head"><div><span class="eyebrow">CICLOS DE ESTUDO</span><h2>Volume e eficiência</h2></div></div><div class="bars advanced">${sorted.map(x=>`<div class="bar-row"><div class="bar-label"><strong>${esc(x.name)}</strong><small>${fmt(x.hits)} acertos · ${fmt(x.errors)} erros</small></div><div class="bar-area"><div class="bar-track"><div class="bar-fill" style="width:${(x.questions/max*100).toFixed(1)}%"></div></div><div class="accuracy-marker" style="left:${Math.min(100,x.accuracy)}%" title="${pct(x.accuracy)}"></div></div><div class="bar-stat"><strong>${fmt(x.questions)}</strong><small>${pct(x.accuracy)}</small></div></div>`).join('')}</div></section><aside class="panel"><span class="eyebrow">FRENTES ATUAIS</span><h2>TDAS x EDAS</h2><div class="compare-card primary"><div><small>TDAS 202</small><strong>${pct(m.tdas.accuracy)}</strong></div><span>${fmt(m.tdas.questions)} questões</span><div class="progress large"><i style="width:${m.tdas.accuracy}%"></i></div><p>${m.tdas.stepsDone}/${m.tdas.stepsTotal} etapas · ${m.tdas.errorNotebook} erros catalogados · ${m.tdas.simulations} simulados.</p></div><div class="compare-card"><div><small>EDAS 400</small><strong>${pct(m.edas.accuracy)}</strong></div><span>${fmt(m.edas.questions)} questões</span><div class="progress large"><i style="width:${m.edas.accuracy}%"></i></div><p>${m.edas.errorNotebook} erros catalogados · ${m.edas.caseStudies} estudos de caso.</p></div><div class="callout"><b>Separação obrigatória:</b> EDAS não entra no consolidado histórico enquanto não houver reconciliação de sobreposição.</div></aside></div>`}
function audit(){const warnings=DATA.meta.syncWarnings||[];return `${title('Fontes e auditoria','O site é camada de apresentação. A fonte primária permanece no Notion vivo.','03.1 + 03.2 · FONTES, AUDITORIA E RECONCILIAÇÃO')}<div class="audit-health ${warnings.length?'warn':''}"><div><span class="dot ${warnings.length?'warn-dot':'live'}"></span><div><small>Estado da sincronização</small><strong>${warnings.length?'Sincronização com ressalvas':'Sincronização íntegra'}</strong></div></div><div><small>Última geração</small><strong>${new Date(DATA.meta.generatedAt).toLocaleString('pt-BR',{timeZone:'America/Sao_Paulo'})}</strong></div><div><small>Alertas</small><strong>${warnings.length}</strong></div></div><section class="panel truth-panel"><div class="panel-head"><div><span class="eyebrow">CADEIA DE VERDADE</span><h2>Quem prevalece quando há divergência</h2></div></div><div class="truth-chain">${DATA.governance.truthChain.map((x,i)=>`<div class="truth-node ${i===0?'primary':''}"><span>0${i+1}</span><strong>${esc(x)}</strong>${i<DATA.governance.truthChain.length-1?'<i>→</i>':''}</div>`).join('')}</div><div class="rule-list compact">${DATA.governance.rules.map((x,i)=>`<div><span>${String(i+1).padStart(2,'0')}</span><p>${esc(x)}</p></div>`).join('')}</div></section><section class="panel"><div class="panel-head"><div><span class="eyebrow">FONTES VIGENTES</span><h2>Mapa operacional</h2></div><a class="text-button" href="${esc(DATA.meta.sourceUrl)}" target="_blank" rel="noreferrer">Abrir Notion ↗</a></div><div class="source-grid">${DATA.governance.sources.map(x=>`<article class="source-card"><div><span class="source-icon">◇</span><div><strong>${esc(x.name)}</strong><code>${esc(x.id)}</code></div></div><span class="pill good">${esc(x.status)}</span></article>`).join('')}</div></section>`}
function strategy(){return `${title('Estratégia de carreira','Entrar, consolidar e subir sem desperdiçar o capital acumulado.','04 · ESTRATÉGIA E PRÓXIMAS ETAPAS')}<section class="strategy-hero panel"><span class="eyebrow">DECISÃO VIGENTE</span><h2>${esc(DATA.strategy.current)}</h2><p>Até a prova, a decisão é operacional. Depois da prova, a decisão volta a ser de carreira.</p></section><div class="strategy-stage-grid">${DATA.strategy.stages.map((x,i)=>`<article class="stage-card"><span class="stage-number">0${i+1}</span><span class="eyebrow">${esc(x.period)}</span><h2>${esc(x.title)}</h2><p>${esc(x.goal)}</p><div class="stage-line"></div></article>`).join('')}</div><div class="dashboard-grid"><section class="panel"><span class="eyebrow">CRITÉRIOS DE DECISÃO</span><h2>O que deve pesar na próxima escolha</h2><div class="decision-grid">${['Reaproveitamento do conteúdo','Chance de nomeação','Remuneração total','Qualidade de vida','Localização','Custo de oportunidade'].map((x,i)=>`<div><span>${String(i+1).padStart(2,'0')}</span><strong>${x}</strong></div>`).join('')}</div></section><section class="panel"><span class="eyebrow">PRINCÍPIOS</span><h2>Regras que protegem a estratégia</h2><div class="principles big">${DATA.strategy.principles.map(x=>`<span class="pill">${esc(x)}</span>`).join('')}</div></section></div>`}
const views={home,journey,exams,finance,performance,audit,strategy};
const viewNames={home:'Visão executiva',journey:'Jornada e marcos',exams:'Concursos e provas',finance:'Investimentos',performance:'Desempenho',audit:'Fontes e auditoria',strategy:'Estratégia de carreira'};
function bindDynamic(){$$('[data-nav]').forEach(el=>el.onclick=()=>navigate(el.dataset.nav));}
function render(){if(!DATA)return;const view=views[currentView]||home;$('#content').innerHTML=view();$('#pageContext').textContent=viewNames[currentView];$('#snapshotDate').textContent=`Home ${dateBR(DATA.meta.homeSnapshot)} · desempenho ${dateBR(DATA.meta.performanceCut)}`;$('#syncText').textContent=DATA.meta.live?'Notion vivo → reconciliação → GitHub Pages':'Snapshot auditado';$('#sideSync').textContent=DATA.meta.live?'Notion conectado':'Snapshot local';$('#sideSnapshot').textContent=`Atualizado ${dateBR(DATA.meta.homeSnapshot)}`;$$('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===currentView));bindDynamic();window.scrollTo({top:0,behavior:'instant'});}
function navigate(view){if(!views[view])return;currentView=view;history.replaceState(null,'',`#${view}`);closeSidebar();render();}
function closeSidebar(){$('#sidebar').classList.remove('open');$('#sidebarBackdrop').classList.remove('show');document.body.classList.remove('menu-open')}
function openSidebar(){$('#sidebar').classList.add('open');$('#sidebarBackdrop').classList.add('show');document.body.classList.add('menu-open')}
function buildSearchIndex(){const out=[];const add=(view,title,text,path='')=>out.push({view,title,text:String(text??''),path});DATA.timeline?.forEach(x=>add('journey',x.title,x.detail,x.date));DATA.exams?.forEach(x=>add('exams',x.name,`${x.role} ${x.status} ${x.ranking}`,x.date));DATA.historyCycles?.forEach(x=>add('performance',x.name,`${x.questions} questões ${x.accuracy}%`,'ciclo'));DATA.governance?.sources?.forEach(x=>add('audit',x.name,`${x.id} ${x.status}`,'fonte'));DATA.strategy?.stages?.forEach(x=>add('strategy',x.title,x.goal,x.period));Object.entries(DATA.metrics||{}).forEach(([group,val])=>Object.entries(val||{}).forEach(([k,v])=>add(group==='finance'?'finance':'performance',`${group}.${k}`,v,'indicador')));searchIndex=out;}
function search(q){const box=$('#searchResults');q=q.trim().toLowerCase();if(!q){box.innerHTML='<div class="command-empty"><strong>Busca global</strong><span>Pesquise concurso, indicador, marco, banco, estratégia ou valor.</span></div>';return}const hits=searchIndex.filter(x=>`${x.title} ${x.text} ${x.path}`.toLowerCase().includes(q)).slice(0,24);box.innerHTML=hits.length?hits.map(x=>`<button class="command-result" data-search-view="${x.view}"><span class="command-icon">${iconFor(x.view)}</span><div><strong>${esc(x.title)}</strong><small>${esc(x.text).slice(0,180)}</small></div><span class="command-path">${esc(viewNames[x.view])}</span></button>`).join(''):'<div class="command-empty"><strong>Nenhum resultado</strong><span>Tente outro termo.</span></div>';$$('[data-search-view]').forEach(b=>b.onclick=()=>{hideSearch();navigate(b.dataset.searchView)})}
function showSearch(){const o=$('#commandPalette');o.classList.remove('hidden');requestAnimationFrame(()=>o.classList.add('show'));setTimeout(()=>$('#searchInput').focus(),30);search('')}
function hideSearch(){const o=$('#commandPalette');o.classList.remove('show');setTimeout(()=>o.classList.add('hidden'),150);$('#searchInput').value=''}
async function loadData(force=false){try{const r=await fetch(`data/snapshot.json${force?'?t='+Date.now():''}`,{cache:force?'no-store':'default'});if(!r.ok)throw new Error(`HTTP ${r.status}`);DATA=await r.json();localStorage.setItem('planoSnapshot',JSON.stringify(DATA));buildSearchIndex();render();toast(force?'Dados atualizados do snapshot publicado.':'Plano carregado.')}catch(e){const cached=localStorage.getItem('planoSnapshot');if(cached){DATA=JSON.parse(cached);buildSearchIndex();render();toast('Modo offline: usando último corte salvo.')}else $('#content').innerHTML='<div class="panel error-panel"><h2>Não foi possível carregar o Plano de Transição.</h2><p>Tente atualizar a página quando a conexão retornar.</p></div>'}}
function init(){const hash=location.hash.replace('#','');if(views[hash])currentView=hash;$$('[data-view]').forEach(b=>b.onclick=()=>navigate(b.dataset.view));$('#menuBtn').onclick=openSidebar;$('#dockMore').onclick=openSidebar;$('#closeMenuBtn').onclick=closeSidebar;$('#sidebarBackdrop').onclick=closeSidebar;$('#refreshBtn').onclick=()=>loadData(true);$('#themeBtn').onclick=()=>{document.documentElement.classList.toggle('light');localStorage.setItem('theme',document.documentElement.classList.contains('light')?'light':'dark')};if(localStorage.getItem('theme')==='light')document.documentElement.classList.add('light');$('#searchTrigger').onclick=showSearch;$('#commandPalette').onclick=e=>{if(e.target===$('#commandPalette'))hideSearch()};$('#searchInput').oninput=e=>search(e.target.value);document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();showSearch()}if(e.key==='Escape'){hideSearch();closeSidebar()}});$('#exportBtn').onclick=()=>{if(!DATA)return;const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(DATA,null,2)],{type:'application/json'}));a.download=`plano-transicao-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)};window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;$('#installBtn').classList.remove('hidden')});$('#installBtn').onclick=async()=>{if(!deferredPrompt)return;deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;$('#installBtn').classList.add('hidden')};function tick(){if($('#clock'))$('#clock').textContent=new Intl.DateTimeFormat('pt-BR',{timeZone:'America/Sao_Paulo',hour:'2-digit',minute:'2-digit',second:'2-digit'}).format(new Date())}tick();setInterval(tick,1000);function online(){const ok=navigator.onLine;$('#offlineState').textContent=ok?'online':'offline';$('#offlineState').className=ok?'online':'offline'}window.addEventListener('online',online);window.addEventListener('offline',online);online();if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js');loadData()}
document.addEventListener('DOMContentLoaded',init);
